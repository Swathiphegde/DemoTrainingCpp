1) const keyword applied to the 'token' on immediate left.
   If there is nothing to its left const is applied to the token on its immediate right.
   
2) While working with pointers treat * as token.